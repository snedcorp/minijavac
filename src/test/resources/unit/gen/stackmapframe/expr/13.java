class Test {
    void main(boolean b) {
        float[] arr = new float[1];
        int r = get(1.0, arr[0], 2.0 + 2.0, !b);
    }

    int get(float i, float j, float k, boolean b) {
        return 1;
    }
}